package com.fasla.doorcontrol.features.auth.domain.usecase;

/**
 * LoginUseCase — encapsulates login business logic.
 * TODO: Inject AuthRepository, validate inputs, call repository, return result.
 */
public class LoginUseCase {
    // TODO: private final AuthRepository repository;
    // TODO: public LoginResult execute(String email, String password) { ... }
}
