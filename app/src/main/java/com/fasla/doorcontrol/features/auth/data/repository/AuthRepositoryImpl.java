package com.fasla.doorcontrol.features.auth.data.repository;

import com.fasla.doorcontrol.features.auth.domain.repository.AuthRepository;

/**
 * AuthRepositoryImpl — implements AuthRepository by orchestrating
 * remote API calls (AuthApiService) and local storage (AuthPreferences).
 * TODO: Inject AuthApiService + AuthPreferences, implement all methods.
 */
public class AuthRepositoryImpl implements AuthRepository {
    // TODO: private final AuthApiService apiService;
    // TODO: private final AuthPreferences preferences;
}
