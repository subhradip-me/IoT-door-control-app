package com.fasla.doorcontrol.features.auth.presentation.viewmodel;

/**
 * AuthViewModel — holds auth UI state (login, register, forgot password).
 * TODO: Extend AndroidViewModel, inject AuthRepository, expose LiveData.
 */
public class AuthViewModel {
    // TODO: Implement ViewModel with LiveData for auth states
}
