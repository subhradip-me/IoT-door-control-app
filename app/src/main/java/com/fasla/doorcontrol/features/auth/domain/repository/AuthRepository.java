package com.fasla.doorcontrol.features.auth.domain.repository;

/**
 * AuthRepository — contract for all authentication operations.
 * TODO: Add methods for login, register, logout, resetPassword, refreshToken.
 */
public interface AuthRepository {

    // TODO: boolean login(String email, String password);
    // TODO: boolean register(String name, String email, String password);
    // TODO: boolean resetPassword(String email);
}
