<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Bluetooth Permissions ─────────────────────────────────────────── -->

    <!-- API 30 and below -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- API 31+ (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="31" />

    <!-- Required for device discovery on API 28 and below -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Required to keep socket alive when screen turns off -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Foreground service for persistent BT connection -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE"
        tools:targetApi="34" />

    <!-- Feature declaration: app requires Bluetooth -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoorControl">

        <!-- Entry Point -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth Feature -->
        <activity android:name=".features.auth.presentation.ui.LoginActivity" />
        <activity android:name=".features.auth.presentation.ui.RegisterActivity" />
        <activity android:name=".features.auth.presentation.ui.ForgotPasswordActivity" />

        <!-- Home Feature -->
        <activity android:name=".features.home.presentation.ui.HomeActivity" />

        <!-- Settings Feature -->
        <activity android:name=".features.settings.presentation.ui.SettingsActivity" />

        <!-- Bluetooth Feature -->
        <activity android:name=".features.bluetooth.presentation.ui.BluetoothActivity" />

        <!-- Bluetooth Service — foreground, persists connection in background -->
        <service
            android:name=".features.bluetooth.presentation.service.BluetoothService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

    </application>

</manifest>